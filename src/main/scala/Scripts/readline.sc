val lines = io.Source.stdin.getLines()

val readDrop = io.Source.stdin.getLines.drop(1)

val readDropSum = io.Source.stdin.getLines.drop(1).next().map(_.toInt).sum

val oneLine = io.StdIn.readLine

val oneInt = io.StdIn.readInt

import java.util.Scanner

val scan = new Scanner(System.in)
val n = scan.nextInt

